# M0 acceptance record

- Milestone: M0 project baseline and decision lock
- Prepared: 2026-08-19
- Current verdict: **M0-03 ACCEPTED; M0-04 PASS / DONE; M0 MILESTONE NOT YET COMPLETE**
- Reviewer/date: Founder, 2026-08-19 (instruction to continue the plan)

## Task evidence

| Task | Result | Evidence |
|---|---|---|
| M0-00 | PASS within read-only scope | [Environment inventory](../environment/m0-inventory.md) includes every requested host/tool/resource/port category, limitations, and authorization-needed list. |
| M0-01 | PASS | Local Git `main`, `.gitignore`, `.env.example`, planned directories/placeholders; Git secret-name/content checks and status captured during self-review. |
| M0-02 | PASS | [AGENTS.md](../../AGENTS.md), [README](../../README.md), [decisions](../../DECISIONS.md), [Backlog](../../BACKLOG.md), and [baseline manifest](../baseline/README.md) with three verified SHA-256 hashes. |
| M0-03 | PASS / ACCEPTED | [PRD](../product/prd-mvp.md), [system context](../architecture/system-context.md), [data model](../architecture/data-model.md), [lifecycle](../specs/content-lifecycle.md), and [ADRs](../adr/README.md); acceptance authority recorded as D-010. |
| M0-04 | PASS / DONE | uv `0.12.3` with CPython `3.12.13`, Node 24/npm 11, `uv.lock`, `package-lock.json`, minimal API/web shells, [ADR-0005](../adr/0005-pinned-toolchain-and-ci-gates.md), and read-only-permission GitHub Actions workflow. In the working tree, a fresh local clone, and [hosted CI run #3](https://github.com/letstry-a11y/agent-ip-os/actions/runs/32333631269), locked restore, `npm run check`, and `npm run build` passed: 4 Python tests reached 100% branch coverage, 1 Vitest passed, and the Next.js production build succeeded. Live `/healthz` returned HTTP 200 with external effects disabled. |

## Constraints and exceptions

- M0-00 through M0-03 made no dependency or application changes. M0-04 installed only locked local development dependencies and minimal non-business API/web shells; no Provider/platform/cloud resource was contacted and no real external action was performed.
- Docker/WSL2/FFmpeg are missing; the Windows hypervisor was confirmed active on 2026-08-20. Docker/WSL2 still block M0-05 runtime evidence, and FFmpeg blocks M3-03, until separately authorized prerequisites exist.
- `npm run check` is the verified one-command M0-04 quality gate. M0-05 now has implemented `stack:up`, `stack:verify`, and `stack:restart` commands, but full-stack acceptance is not claimed until they run successfully on the target host and clean checkout.
- D-009 is decided: the GitHub remote is public, hosted CI passes, and `main` protection is enforced with pull requests plus the up-to-date `quality` check. Collaborator access remains unchanged. Other unresolved human decisions retain their documented safe fallbacks.

## Founder review checklist

- [x] Accept or revise the MVP scope/non-goals and six runtime units.
- [x] Accept or revise the parent/child state model and fail-closed invariants.
- [x] Accept or revise system/data boundaries and ADR-0001..0004.
- [x] Record D-003 direction and safe deferral: identity-derived portrait activation remains blocked pending the signed scope and Provider/storage details; M0-04/M1 may continue with non-identifying synthetic fixtures only.
- [x] Record reviewer, date, verdict, exceptions, and whether M0-04 may proceed.
- [x] Verify locked local installs, the canonical quality gate, production web build, and API health smoke test.
- [x] Authorize the GitHub remote under D-009, capture the first successful hosted CI run, and enforce the protected-`main` pull-request workflow.

## Gate outcome

M0-03 and M0-04 are accepted and `DONE`. M0 remains incomplete until M0-05 meets its own evidence; M0-05 requires the environment prerequisites and explicit machine-change authority described in M0-00.

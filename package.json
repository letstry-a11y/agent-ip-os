{
  "name": "agent-ip-os",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": ">=24.0.0 <25",
    "npm": ">=11.0.0 <12"
  },
  "packageManager": "npm@11.12.1",
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "build": "npm run build --workspace @agent-ip/web",
    "check": "npm run check:repo && npm run check:python && npm run check:web",
    "check:repo": "uv run --locked python scripts/check_repo.py",
    "check:python": "npm run check:python:format && npm run check:python:lint && npm run check:python:types && npm run check:python:test",
    "check:python:format": "uv run --locked ruff format --check apps/api/src tests scripts",
    "check:python:lint": "uv run --locked ruff check apps/api/src tests scripts",
    "check:python:types": "uv run --locked mypy apps/api/src scripts",
    "check:python:test": "uv run --locked pytest --cov=agent_ip_api --cov-report=term-missing --cov-fail-under=100",
    "check:web": "npm run check --workspace @agent-ip/web",
    "dev:api": "uv run uvicorn agent_ip_api.main:app --reload --host 127.0.0.1 --port 8000",
    "dev:web": "npm run dev --workspace @agent-ip/web",
    "format": "uv run ruff format apps/api/src tests scripts && uv run ruff check --fix apps/api/src tests scripts && npm run format --workspace @agent-ip/web"
  }
}
